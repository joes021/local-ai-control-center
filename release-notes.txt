v2.24.14
========

- Windows desktop shortcut ikonice sada eksplicitno koriste `IconLocation=<ico>,0`, pa `.lnk` fajlovi vise ne ostaju bez ikonice.
- Windows desktop folder sada dobija `desktop.ini` i folder ikonicu, umesto generickog praznog folder prikaza.
- Browser download status i pending rezultat vise nisu zakucani skroz ispod tabele, nego stoje odmah ispod detail panela uz model na koji se akcija odnosi.
- Browser download status sada ima i pravi progress bar, pa se ETA/procenat vizuelno vide odmah posle klika na `Download`.
- Benchmark grafikon sada koristi objedinjeni signal iz `history` i `liveHistory`, pa vise ne ostaje prazan kada pokrenes klasicni benchmark test.
- Dodat je `Clear benchmark values` tok koji resetuje benchmark metrike, live istoriju i sacuvane benchmark run vrednosti pre novog merenja.
- Updates fallback sada cita latest release direktno sa `joes021/local-ai-control-center` GitHub release API-ja, sa ispravnim Windows, Ubuntu x86_64 i Ubuntu arm64 installer URL-ovima.
- Update installer nazivi i putanje vise nisu legacy `Local-Qwen-Setup-*`, nego prate stvarne `Local-AI-Control-Center-*` release artefakte.

- Fixed Hugging Face browser size detection by preserving repo slashes in tree API requests, so Browser table sizes now load instead of staying `Unknown`.
- Fixed detected active local models outside the managed models folder so they appear as installed and active in the Models tab.
- Fixed Linux installer payload to package the real legacy Linux launchers plus `install/shared`, and to copy shared `config/` and `scripts/` into the install root so Browser downloads, model activation, and OpenCode launchers work on Ubuntu installs.
- Tightened Linux runtime readiness reporting when an already healthy runtime port is reused during install.
- Fixed Linux launcher patching so Browser `Download` no longer writes a broken OpenCode `baseURL` expression.
- Fixed Linux TUI installer so model selection writes a clean `selectedModelId` instead of capturing the visible menu text.
- Added Linux fallback activation for detected local GGUF models that exist outside the managed models directory, so portal model switching can recover those real local files too.

v2.24.11
- Linux repo fallback sada sadrzi `show-logs.sh` i `local_qwen_common.sh`, pa Log Viewer vise ne pada na missing script.
- Odrzana je kompatibilnost sa novim `~/local-ai-control-center` root-om i starim legacy root-om.

v2.24.9
- Linux installer vise ne sme da prepozna nasumicni ili mmproj GGUF kao aktivni bootstrap model.
- Novi podrazumevani Linux install root je `~/local-ai-control-center`, uz fallback na postojeci `~/local-qwen-home`.
- LAICC nastavlja da prikazuje stvarno aktivan lokalni model kroz detected model fallback i otporniji Browser download ID tok.

v2.24.8
- Linux installer sada prihvata vec aktivan/lokalno prisutan model kao validan bootstrap kandidat, pa reinstall na masini koja vec ima zdrav model vise ne pada na `bootstrap-not-ready`.
- `download-failed` sada cuva stvaran razlog iz bootstrap download koraka, pa installer report vise nije slep ako preporuceni model ne uspe da se preuzme.

v2.24.7
- Browser tabela sada popunjava GGUF velicine preko Hugging Face `tree` API-ja kada osnovni model API ne vrati `siblings.size`, pa velicine vise ne ostaju `Unknown`.
- Browser redovi sada cuvaju pravi `repoId`, pa `Download` iz same tabele i detail panela koristi validan katalog zapis umesto praznog repo identifikatora.
- Direktni link `Open model page` ostaje dostupan iz same Browser tabele.
- Linux `OpenCode` otvaranje iz LAICC ostaje kroz terminal launcher, a ARM Browser fixevi su objedinjeni u isti patch release.

v2.24.6
- Browser refresh vise ne rusi ekran kada se klikne `Refresh from internet`; refresh payload se sada prikazuje kroz bezbedan rezultat umesto kroz pogresan ActionResult shape.
- Browser tabela sada dobija stvarne velicine (`sizeBytes` / `sizeLabel`) iz source kataloga, pa GGUF fajlovi vise ne zavrsavaju kao `Unknown` kada API vrati poznatu velicinu.
- Browser download sada radi i bez prethodnog rucnog `Add to local catalog`: backend registruje model pa odmah pokrece download tok.
- Iz same Browser tabele sada postoji direktan link `Open model page` ka Hugging Face / Unsloth stranici modela.
- Linux installer vise ne pregazi vec aktivan model ako je sistem vec imalo zdravo aktivno runtime stanje; bootstrap model postaje aktivan samo kada aktivni model nije vec razresen.
- Linux `OpenCode` pokretanje iz Control Center-a sada ide kroz `desktop-launch.sh`, tako da se TUI zaista otvara u terminal emulatoru umesto da se poziv izgubi u pozadini.

v2.24.5
- Linux installer model bootstrap vise ne zavisi od legacy `manage-models.sh` mapiranja za obavezni bootstrap korak, nego direktno skida iz zajednickog recommended-models kataloga.
- Linux `OpenCode` launch iz Control Center-a sada koristi isti `start-opencode.sh` launcher tok koji radi i pri rucnom terminal pokretanju, umesto sirovog `opencode` executable poziva.
- GitHub storefront i release linkovi su poravnati na `v2.24.5`.

v2.24.4
- Linux `.run` installer vise ne radi los `GUI pa opet TUI` tok; GUI i fallback su sada razdvojeni.
- Linux dependency bootstrap vise ne pokusava slepo konfliktni `apt install nodejs npm` na NodeSource sistemima.

v2.24.3
- Windows desktop shortcut creation sada eksplicitno dodeljuje `control-center.ico` i `opencode-local-qwen.ico`, pa installer vise ne ostavlja genericke precice bez nasih ikonica.

v2.24.2
- Windows installer vise ne koristi nepostojecu Inno Setup konstantu `{userprofile}`.
- Setup sada bezbedno resava `LocalQwenHome` kroz `USERPROFILE` sa fallback putanjom, pa instalacija vise ne puca na koraku `ssPostInstall`.

v2.24.1
- Windows installer sada registruje preporucene Unsloth modele u legacy katalog pre bootstrap download-a.
- Bootstrapped model se automatski sinhronizuje nazad u aktivni install root, pa first-run probe vise ne pada na Windows clean install toku.
